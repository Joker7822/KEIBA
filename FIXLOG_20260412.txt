修正日: 2026-04-12
対象: keibaAI-v2-main

修正内容
1. modules/preparing/_scrape_race_id_list.py
   - scrape_kaisai_date() を日付範囲対応に修正
   - 受付形式: YYYY/MM/DD, YYYY-MM-DD, YYYYMMDD, YYYY/MM, YYYY-MM
   - 指定期間の両端を含むように修正
   - 終了日が月途中でも、その月を取得対象に含めた上で日単位でフィルタ
   - 開催日重複を除去しソート
   - scrape_race_id_list() に途中CSV保存、継続実行、後始末強化を追加

2. modules/constants/_local_paths.py
   - TMP_DIR を追加

3. run_scrape_date_range.py
   - 2010/01/01 ～ 2026/04/12 をそのまま実行できるCLIスクリプトを追加
   - 開催日CSV / race_id CSV を data/tmp に保存
   - オプションで race HTML の保存にも対応

4. README.md
   - 実行例を追記

実行例
python run_scrape_date_range.py --from-date 2010/01/01 --to-date 2026/04/12
python run_scrape_date_range.py --from-date 2010/01/01 --to-date 2026/04/12 --download-race-html

注意
- この環境では外部サイトへの実スクレイピングは未実行です。
- コード構文チェック（py_compile）は実施済みです。


=== additional fix (2026-04-12 second pass) ===
5. modules/constants/_local_paths.py
   - BASE_DIR を実行カレントディレクトリ依存から、ファイル位置基準のプロジェクトルート固定へ修正

6. modules/preparing/_scrape_race_id_list.py
   - calendar 取得に User-Agent / timeout / retry を追加
   - kaisai_date / race_id の正規化を厳格化（8桁 / 12桁）
   - selenium import を遅延化し、未導入時のエラーメッセージを明確化
   - save_csv_path の dirname が空でも落ちないように修正

7. modules/preparing/_prepare_chrome_driver.py
   - selenium / webdriver-manager の import を遅延化し、未導入時に説明付き ImportError を返すよう修正

8. modules/preparing/__init__.py
   - optional dependency がなくても preparing の基本機能 import が落ちないように修正

9. modules/preparing/_get_rawdata.py
   - `from numpy import NaN` を `from numpy import nan as NaN` に修正
     （NumPy 環境によって import error になる問題を解消）

10. run_scrape_date_range.py
   - scrape_html_race 未読込時のエラーメッセージを明確化
